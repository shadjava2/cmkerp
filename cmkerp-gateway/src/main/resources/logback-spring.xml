<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Propriétés -->
    <springProperty scope="context" name="app.name" source="spring.application.name" defaultValue="cmkerp-gateway"/>
    <springProperty scope="context" name="log.level" source="logging.level.root" defaultValue="INFO"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{correlationId}] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/${app.name}.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{correlationId}] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/${app.name}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Réduction des logs verbeux par défaut -->
    <logger name="org.hibernate.validator" level="WARN"/>
    <logger name="org.springframework.boot.autoconfigure" level="INFO"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.springframework.security" level="INFO"/>
    <logger name="org.springframework.jdbc" level="WARN"/>
    <logger name="org.apache.catalina" level="WARN"/>
    <logger name="org.apache.tomcat" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="WARN"/>
    <logger name="org.springframework.boot.actuate" level="INFO"/>
    <!-- Désactiver les logs SQL détaillés par défaut -->
    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="WARN"/>
    <!-- Réduire les logs de debug pour les requêtes nommées manquantes (comportement normal) -->
    <!-- Spring Data JPA vérifie l'existence de @NamedQuery pour chaque repository -->
    <!-- même si elles n'existent pas (les repositories utilisent des méthodes dérivées ou @Query) -->
    <logger name="org.hibernate.resource.transaction" level="WARN"/>
    <logger name="org.hibernate.internal.AbstractSharedSessionContract" level="WARN"/>
    <logger name="org.springframework.data.jpa.repository.query.NamedQuery" level="WARN"/>

    <!-- Loggers spécifiques du projet -->
    <logger name="cd.shad.erp.cmk.cmkerp" level="INFO"/>

    <!-- Root Logger -->
    <root level="${log.level}">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Profils Spring -->
    <springProfile name="dev,development">
        <logger name="cd.shad.erp.cmk.cmkerp" level="INFO"/>
        <logger name="org.springframework.web.servlet" level="INFO"/>
        <logger name="org.hibernate" level="WARN"/>
        <logger name="org.hibernate.SQL" level="WARN"/>
        <logger name="org.hibernate.orm" level="WARN"/>
        <logger name="org.hibernate.engine" level="WARN"/>
        <logger name="org.hibernate.p" level="WARN"/>
        <logger name="org.hibernate.e.i.StatisticalLoggingSessionEventListener" level="WARN"/>
        <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="WARN"/>
        <logger name="com.zaxxer.hikari" level="WARN"/>
        <logger name="com.zaxxer.hikari.pool" level="WARN"/>
    </springProfile>

    <springProfile name="prod,production">
        <logger name="cd.shad.erp.cmk.cmkerp" level="INFO"/>
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
        <logger name="com.zaxxer.hikari" level="INFO"/>
        <!-- Désactiver les logs SQL détaillés en prod -->
        <logger name="org.hibernate.SQL" level="WARN"/>
        <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="WARN"/>
        <!-- Réduire la verbosité HikariCP en prod -->
        <logger name="com.zaxxer.hikari.pool" level="WARN"/>
        <logger name="com.zaxxer.hikari.util" level="WARN"/>
    </springProfile>
</configuration>

